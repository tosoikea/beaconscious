import 'package:beaconscious/navigation/routes/route_path.dart';

/// Route to the overview of the app
class HomeRoutePath extends RoutePath {
  const HomeRoutePath._internal();

  factory HomeRoutePath.overview() => const HomeRoutePath._internal();
}
